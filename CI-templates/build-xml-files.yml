build-job:
  stage: build-job
  tags:
    - edt
  script:
    - executor .ci-scripts/BuildXmlFiles.sbsl -CliPath "$env:EDT_CLI_EXECUTABLE" -ProjectPath "$env:CI_PROJECT_DIR"
  artifacts:
    untracked: false
    when: on_success
    expire_in: 30 days
    paths:
      - "*.zip"
    
    