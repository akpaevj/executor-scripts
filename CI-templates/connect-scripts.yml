default:
  before_script:
    - git clone https://gitlab-ci-token:$CI_JOB_TOKEN@$CI_SERVER_HOST/$CI_PROJECT_NAMESPACE/ci-scripts.git ".ci-scripts"
  after_script:
    - Remove-Item ".ci-scripts" -Recurse -Force